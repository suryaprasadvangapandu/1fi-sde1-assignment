import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: '1Fi - Smartphones on EMI Backed by Mutual Funds',
  description:
    'Buy iPhone 17 Pro, Samsung S24 Ultra, and top smartphones with 0% No-Cost EMI backed by your mutual fund portfolio. Zero liquidation, instant sanction on 1Fi.',
  icons: {
    icon: '/favicon.ico',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-slate-50 text-slate-900 selection:bg-purple-500 selection:text-white">
        {children}
      </body>
    </html>
  );
}
